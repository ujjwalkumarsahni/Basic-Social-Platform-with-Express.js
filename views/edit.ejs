<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Update Post</title>
</head>
<body class="bg-gray-900 text-white min-h-screen flex items-center justify-center">
    <section class="w-full max-w-lg p-6 rounded shadow bg-gray-800">
        <h2 class="text-2xl font-semibold mb-4 text-center text-yellow-400">Update Your Post</h2>
        <form action="/update/<%= post._id %>" method="POST" class="space-y-4">
            <textarea 
                name="content" 
                placeholder="What's on your mind?" 
                class="w-full border border-gray-700 rounded p-3 focus:outline-none focus:ring-2 focus:ring-yellow-500 bg-gray-700 text-gray-200"
                required><%= post.content %></textarea>
            <button 
                type="submit" 
                class="w-full px-6 py-2 bg-yellow-600 text-white rounded hover:bg-yellow-700 transition">
                Update Post
            </button>
        </form>
    </section>
</body>
</html>
